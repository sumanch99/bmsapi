package com.cts.bms.util;
/*package com.cts.bms.utility;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Date;

import com.cts.bms.model.FixedDeposit;

public class Main {

	public static void main(String[] args) throws ParseException {
		// TODO Auto-generated method stub
		FixedDeposit dep = new FixedDeposit();
		dep.setFixed_deposit_id(01);
		dep.setAccount_number(001);
		dep.setAmount(5000);
		dep.setTenure(120);
	    dep.setStart_date(new Date());
	    dep.setInterest(5);
	    dep.setIsMatured(false);
	    FixedDepositUtility fd = new FixedDepositUtility();
	    System.out.println(fd.getMaturedAmount(5000, 5, 120));
	    for(int i = 1;i<=120;i++)
	    {
	    	fd.pay(dep);
	    }
	    System.out.println(dep.getAmount());
	    System.out.println(dep.getMatured_amount());
	}

}
*/